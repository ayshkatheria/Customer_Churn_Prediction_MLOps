**STEP 7 — Push Project to GitHub**

Create GitHub repo:
```
customer-churn-mlops
```

Then:
```
git add .
git commit -m "Add churn dataset tracking"

git remote add origin <your-github-url>

git push -u origin main
```

🎉 Now You Have Real MLOps Setup

✅ Code → GitHub
✅ Data → S3
✅ Dataset metadata → Git
✅ Version tracking → DVC
